---
interface Props {
  t: any;
}

const { t } = Astro.props;
const year = new Date().getFullYear();
---

<footer class="text-center py-8 px-6 text-xs relative z-10 border-t" style={`color: var(--t-text-light); border-color: var(--t-border);`}>
  <p>© {year} webioinfo · {t}</p>
</footer>
